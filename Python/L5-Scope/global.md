# global key word

- What does the global keyword actually do?
- Does it create a new variable?
- Does it create a new object?
- Does it change the search order?
- Why should global be used sparingly?

## Without global:

- Assignment inside a function -> Creates/uses a local variable

## With global:

- Assignment inside a function -> Updates the global variable
